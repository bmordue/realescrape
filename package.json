{
  "name": "realescrape",
  "version": "0.0.1",
  "scripts": {
    "prestart": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "start": "node built/app.js",
    "heatmap:data": "tsc && node built/heatmap/generate.js",
    "heatmap:render": "tsc && node built/heatmap/render.js",
    "heatmap": "tsc && node built/heatmap/generate.js && node built/heatmap/render.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1",
    "node-fetch": "^2.6.7",
    "openai": "^4.14.0",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^18.19.123",
    "@types/node-fetch": "^2.6.2",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.1"
  }
}
